#spring:
#  resources:
#    add-mappings: false
#  mvc:
#    throw-exception-if-no-handler-found: true

management:
  endpoints:
    enabled-by-default: false

spring:
  profiles:
    include: management, gcp

interledger:
  spsp-server:
    keys:
      secret0:
        alias: secret0
        version: 1
      accountSettings:
        alias: secret0
        version: 1
